enum ACTIONS {
  EDIT
  DELETE
}

type Mutation {
  editNotification(
    noficationId: String!
    title: String!
    text: String!
    url: String
    timeCreate: String!
    timeLimit: String!
    actions: ACTIONS!
  ): Boolean!
}
